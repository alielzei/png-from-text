# Node server returns base64 png Image of text

## Usage:

yournodeserverinstance.com/?text=string&size=number&font=font

## Fonts:

To add a font add it in the /resources folder with .ttf extension 

